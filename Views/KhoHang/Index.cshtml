@model IEnumerable<ShopGiay.Models.PHIEUNHAPKHO>

@{
    ViewBag.Title = "Kho";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>KHO NHẬP</h2>
<table class="table">
    <tr>
        <th>
            Ngày nhập:
        </th>
        <th>
           Mã giày:
        </th>
        <th>
            Số lượng:
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NGAYNK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAGIAY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SOLUONG)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MAPHIEUNK }) |
            @Html.ActionLink("Chi tiết", "Details", new { id=item.MAPHIEUNK }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MAPHIEUNK })
        </td>
    </tr>
}

</table>
