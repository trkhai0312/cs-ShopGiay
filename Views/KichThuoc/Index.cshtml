@using ShopGiay.Models
@using PagedList.Mvc;
@model PagedList.PagedList<ShopGiay.Models.KICHTHUOC>

@{
    ViewBag.Title = "Trang quản lý kích thước giày";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>TRANG QUẢN LÝ KÍCH THƯỚC GIÀY</h2>
<table class="table">
    <tr>
        <th>Mã giày</th>
        <th> Tên Giày </th>
        <th> Kích Thước </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GIAY.MAGIAY)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GIAY.TENGIAY)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TENKICHTHUOC)
            </td>
            <td>
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MAKICHTHUOC }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.MAKICHTHUOC })
            </td>
        </tr>
    }

</table>
<div>
    Trang @(@Model.PageNumber>Model.PageCount?Model.PageCount: Model.PageNumber)/@Model.PageCount
</div>
<div align="center">@Html.PagedListPager(Model, page => Url.Action("Index", new { page = page }))</div>
<link href="~/Content/phantrang.css" rel="stylesheet" />
